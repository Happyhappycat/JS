"use strict" // treat all JS code as newer version
// alert(3+3) // we are using node.js not browser

//datatypes- number,string,boolean, Null, undefined, symbol

let dtype=typeof("Harshavardhan")
console.log(dtype)